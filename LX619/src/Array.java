import java.util.Arrays;

public class Array {
    public static void main(String[] args) {
        int[] a={12,1,21,54,2,5,54,255,2884,5};
        Sort.sort(a);
        /*int[] ints = Arrays.copyOf(a, 10);
       Arrays.sort(a);
       int min=0;
       int max=0;
        for (int i = 0; i < ints.length; i++) {
            if (ints[i] == a[0]){
                min=i;
            }
            if (ints[i] == a[a.length-1]){
                max=i;
            }
        }
        int t=ints[min];
        ints[min]=ints[max];
        ints[max]=t;
        for (int i = 0; i < ints.length; i++) {
            System.out.print(ints[i]+" ");
        }*/
    }

}
